# Overridden temporarily to get Salsa-CI pass and not stall other dev work with broken pipeline
wrong-path-for-interpreter usr/share/mysql/mysql-test/plugin/rocksdb/rocksdb/slow_query_log.awk (#!/bin/awk != /usr/bin/awk)
